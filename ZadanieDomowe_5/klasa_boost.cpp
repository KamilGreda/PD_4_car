#include "klasa_boost.h"

C_boost::C_boost(double _x, double _y, double _a, double _b, double _red, double _green, double _blue)
{
	x = _x;
	y = _y;
	a = _a;
	b = _b;
	red = _red;
	green = _green;
	blue = _blue;
}

void C_boost::Draw()
{
	/*
	x = car.x + R*sin(car.angle*M_PI / 180);
	y = car.y + R*cos(car.angle*M_PI / 180);
	y += sin((car.angle / 2)* M_PI / 180) * car.a / 2;
	x += cos((car.angle / 2)* M_PI / 180)*car.a / 2;
	*/


}